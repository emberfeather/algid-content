/**
 * Content JavaScript
 * 
 * Used to unobtrusively enhance the content experience for the user.
 */
;(function($) {
	$( function() {
		// Init any datagrids
		$('.datagrid').datagrid();
	});
})(jQuery);
